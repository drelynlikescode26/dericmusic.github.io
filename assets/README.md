Place the main hero image here as `hero.jpg`.

Instructions:
1. Save the attached portrait image file to this folder.
2. Name it `hero.jpg` (lowercase) so the site references it automatically.

If you prefer PNG or WebP, update `style.css` background URL accordingly.

Preview locally with:

```bash
python3 -m http.server 8000
```

Open http://localhost:8000
